# Database configuration for Docker Compose
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=


# Backend configuration
# PostgreSQL connection (replace with real credentials)
DATABASE_URL=

# App
PORT=
NODE_ENV=

# Auth
JWT_SECRET=
BCRYPT_SALT_ROUNDS=